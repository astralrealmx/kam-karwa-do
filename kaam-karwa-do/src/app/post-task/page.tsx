import { PageShell } from "@/components/layout/PageShell";

export default function PostTaskPage() {
  return (
    <PageShell
      title="Post a Task"
      subtitle="The full task-posting flow (category, description, location, timing, budget) will be built in Phase 2. This route exists so navigation and CTAs have a real destination in Phase 1."
    />
  );
}
